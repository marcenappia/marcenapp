import type{IntentResolver,IntentResolverInput,ResolvedIntent,IntentName}from"./types";import{buildToolDeclarations}from"../tools/schema";import{planWithFallback,type ProviderRouterConfig}from"../providers/providerRouter";const MAP:Record<string,IntentName>={createCliente:"create_cliente",createProjeto:"create_projeto",gerarRender:"gerar_render",calcularOrcamento:"calcular_orcamento",gerarContrato:"gerar_contrato",operationalIntelligence:"operational_intelligence",iaraSmartAction:"smart_action"};export const createLlmResolver=(router:ProviderRouterConfig):IntentResolver=>({name:"llm",async resolve(input:IntentResolverInput):Promise<ResolvedIntent|null>{const r=await planWithFallback({systemPrompt:"Você é a IARA. Nunca invente dados; use somente as ferramentas disponíveis e peça apenas campos obrigatórios ausentes.",userPrompt:input.text,tools:buildToolDeclarations(),contextBlock:`\\nCONTEXTO:\\n${JSON.stringify({projectId:input.context.projectId,environmentId:input.context.environmentId,conversation:input.context.recentMessages.slice(-12)})}`},router);const first=r.plan[0];if(!first)return{intent:"unknown",entities:{},confidence:.3,missingSlots:[],source:"llm",summary:r.summary};return{intent:MAP[first.tool]??"unknown",entities:first.args,confidence:.8,missingSlots:[],source:"llm",summary:r.summary}}});
