import{describe,it,expect}from"vitest";import{deterministicResolver}from"./deterministicResolver";
const ctx={projectId:undefined,environmentId:undefined,recentMessages:[]};
describe("IARA deterministic resolver",()=>{it("reconhece projeto com dimensões",async()=>{const r=await deterministicResolver.resolve({text:"crie um armário 240x220x60",context:ctx,correlationId:"t"});expect(r?.intent).toBe("create_projeto");expect(r?.missingSlots).toHaveLength(0);expect(r?.entities.width).toBe(240)});it("mantém slots faltantes estruturados",async()=>{const r=await deterministicResolver.resolve({text:"crie um armário",context:ctx,correlationId:"t"});expect(r?.intent).toBe("create_projeto");expect(r?.missingSlots.map(x=>x.field)).toEqual(["width","height","depth"])});it("reconhece render por texto",async()=>{const r=await deterministicResolver.resolve({text:"renderize este projeto",context:ctx,correlationId:"t"});expect(r?.intent).toBe("gerar_render")});it("não chama heurística para imagem",async()=>{const r=await deterministicResolver.resolve({text:"analise isso",images:[{mimeType:"image/png",data:"x"}],context:ctx,correlationId:"t"});expect(r).toBeNull()})});
