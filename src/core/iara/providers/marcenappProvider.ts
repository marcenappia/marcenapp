import{callAIFunction}from"@/services/ai";import type{AIProvider,PlanToolsParams,ProviderPlanResult}from"./aiProvider";export const marcenappProvider:AIProvider={id:"lovable",async planTools(p:PlanToolsParams):Promise<ProviderPlanResult>{const data=await callAIFunction<{plan?:Array<{tool:string;args:Record<string,unknown>}>;summary?:string;model?:string}>("ai-orchestrator",{userPrompt:p.userPrompt,context:p.contextBlock?{conversation:[]}:undefined});return{plan:data.plan??[],summary:data.summary??"",model:data.model??"server"}}};
