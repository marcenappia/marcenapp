import React, { useState, useRef, useEffect } from 'react';
import { X, MessageCircle, FolderKanban, UserRound, CheckCircle2 } from 'lucide-react';
import { ChatMessages } from './components/ChatMessages';
import { ChatInput, type SmartAction } from './components/ChatInput';
import { ContextPanel, type ContextPanelData } from './components/ContextPanel';
import AuthDialog from '../../components/marcenaria/AuthDialog';
import { useIaraChat } from './hooks/useIaraChat';

interface IaraModuleProps {
  syncProject?: { width?: number; height?: number; depth?: number } | null;
  onProjectChange?: (p: { width: number; height: number; depth: number }) => void;
  embedded?: boolean;
  projectId?: string | null;
  projectName?: string | null;
  clientName?: string | null;
  environment?: string | null;
  status?: string | null;
}

const IaraModule = ({ syncProject, onProjectChange, embedded, projectId = null, projectName, clientName, environment, status }: IaraModuleProps = {}) => {
  const [showAuthDialog, setShowAuthDialog] = useState(false);
  const [contextPanel, setContextPanel] = useState<ContextPanelData | null>(null);
  const [factors, setFactors] = useState({ L: syncProject?.width ?? 2.4, A: syncProject?.height ?? 2.6, P: syncProject?.depth ?? 0.6, E: 0.018, X: 0, Y: 0 });
  const [decorStyle] = useState('Limpo');
  const [activeImageZoom, setActiveImageZoom] = useState<{ url: string; budget?: string | null } | null>(null);
  useEffect(() => { if (!syncProject) return; setFactors(prev => { const next = { ...prev, L: syncProject.width ?? prev.L, A: syncProject.height ?? prev.A, P: syncProject.depth ?? prev.P }; if (next.L === prev.L && next.A === prev.A && next.P === prev.P) return prev; return next; }); }, [syncProject?.width, syncProject?.height, syncProject?.depth]);
  useEffect(() => { if (!onProjectChange) return; const sameAsStudio = syncProject?.width === factors.L && syncProject?.height === factors.A && syncProject?.depth === factors.P; if (sameAsStudio) return; onProjectChange({ width: factors.L, height: factors.A, depth: factors.P }); }, [factors.L, factors.A, factors.P, onProjectChange, syncProject?.width, syncProject?.height, syncProject?.depth]);
  const { messages, chatInput, setChatInput, isTyping, isListening, handleSend, handleSmartAction, handleImageSelect, toggleRecording, maskingImage, setMaskingImage, pendingUpload, setPendingUpload, error, retryLast, dismissError } = useIaraChat(factors, decorStyle, setShowAuthDialog, { onProjectCreated: (p) => setFactors(prev => ({ ...prev, L: p.width ?? prev.L, A: p.height ?? prev.A, P: p.depth ?? prev.P })) }, projectId);
  const lastPushedRef = useRef<string | null>(null); const isDrawingRef = useRef(false);
  const messagesEndRef = useRef<HTMLDivElement>(null); const canvasRef = useRef<HTMLCanvasElement>(null); const ctxRef = useRef<CanvasRenderingContext2D | null>(null);
  useEffect(() => { messagesEndRef.current?.scrollIntoView({ behavior: 'smooth' }); }, [messages, isTyping]);
  useEffect(() => { if (maskingImage && canvasRef.current) { const c = canvasRef.current; c.width = 1080; c.height = 1920; const ctx = c.getContext('2d'); if (ctx) { ctx.lineCap = 'round'; ctx.lineJoin = 'round'; ctx.strokeStyle = 'rgba(255, 255, 255, 0.85)'; ctx.lineWidth = 60; ctxRef.current = ctx; } } }, [maskingImage]);
  type CanvasPointerEvent = React.MouseEvent<HTMLCanvasElement> | React.TouchEvent<HTMLCanvasElement>;
  const getCoords = (e: CanvasPointerEvent) => { const rect = canvasRef.current?.getBoundingClientRect(); if (!rect) return null; const sx = 1080 / rect.width; const sy = 1920 / rect.height; const point = 'touches' in e ? e.touches[0] : e; if (!point) return null; return { offsetX: (point.clientX - rect.left) * sx, offsetY: (point.clientY - rect.top) * sy }; };
  const startDraw = (e: CanvasPointerEvent) => { if (!ctxRef.current) return; const c = getCoords(e); if (!c) return; ctxRef.current.beginPath(); ctxRef.current.moveTo(c.offsetX, c.offsetY); isDrawingRef.current = true; };
  const moveDraw = (e: CanvasPointerEvent) => { if (!isDrawingRef.current || !ctxRef.current) return; const c = getCoords(e); if (!c) return; ctxRef.current.lineTo(c.offsetX, c.offsetY); ctxRef.current.stroke(); };
  const endDraw = () => { isDrawingRef.current = false; };
  const confirmMask = async () => { if (!maskingImage || !canvasRef.current) return; const canvas = document.createElement('canvas'); canvas.width = 1080; canvas.height = 1920; const ctx = canvas.getContext('2d'); if (!ctx) return; ctx.fillStyle = '#000000'; ctx.fillRect(0, 0, 1080, 1920); const ratio = Math.min(1080 / maskingImage.img.width, 1920 / maskingImage.img.height); const dw = maskingImage.img.width * ratio; const dh = maskingImage.img.height * ratio; ctx.drawImage(maskingImage.img, (1080 - dw) / 2, (1920 - dh) / 2, dw, dh); const baseB64 = canvas.toDataURL('image/jpeg', 0.7); const baseRaw = baseB64.split(',')[1]; ctx.clearRect(0, 0, 1080, 1920); ctx.fillStyle = '#000000'; ctx.fillRect(0, 0, 1080, 1920); ctx.drawImage(canvasRef.current, 0, 0); const idata = ctx.getImageData(0, 0, 1080, 1920); const d = idata.data; for (let i = 0; i < d.length; i += 4) { if (d[i + 3] > 10) { d[i] = d[i + 1] = d[i + 2] = 255; d[i + 3] = 255; } else { d[i] = d[i + 1] = d[i + 2] = 0; d[i + 3] = 255; } } ctx.putImageData(idata, 0, 0); const maskRaw = canvas.toDataURL('image/png').split(',')[1]; setPendingUpload({ base64: baseB64, baseRaw, maskRaw }); setMaskingImage(null); };
  const handleChatInputChange = (value: React.SetStateAction<string>) => { setChatInput(value); };
  const openArtifact = (data: { type: string; id?: string; imageUrl?: string | null }) => setContextPanel({ title: data.type === 'render' ? 'Render do projeto' : data.type === 'budget' ? 'Orçamento do projeto' : 'Artefato do projeto', type: data.type, imageUrl: data.imageUrl, description: projectId ? 'Este contexto está vinculado ao projeto atual e permanece disponível sem sair da conversa.' : undefined });
  const onSmartAction = (action: SmartAction) => void handleSmartAction(action);
  const displayProjectName = projectName?.trim() || 'Projeto atual';
  const displayClientName = clientName?.trim();
  const displayEnvironment = environment?.trim();
  const displayStatus = status?.trim();
  return (<div className={`flex flex-col ${embedded ? 'h-full' : 'h-[calc(100vh-8rem)] md:h-[calc(100vh-4rem)]'} bg-background relative overflow-hidden rounded-2xl border border-slate-200 shadow-sm`}><header className="px-4 py-3 bg-white border-b border-slate-200 flex items-center justify-between gap-4 shrink-0"><div className="flex items-center gap-3 min-w-0"><div className="w-9 h-9 rounded-xl bg-slate-100 border border-slate-200 flex items-center justify-center shrink-0"><MessageCircle size={18} className="text-slate-700" aria-hidden="true" /></div><div className="min-w-0"><div className="flex items-center gap-2 min-w-0"><h2 className="text-sm font-bold text-slate-900 leading-none truncate">IARA</h2>{projectId && <span className="inline-flex items-center gap-1 rounded-full bg-emerald-50 px-2 py-0.5 text-[9px] font-bold text-emerald-700 shrink-0"><span className="h-1.5 w-1.5 rounded-full bg-emerald-500" aria-hidden="true" />Conectada</span>}</div><p className="text-[10px] text-slate-500 mt-1 truncate">{displayProjectName}{displayClientName ? ` · ${displayClientName}` : ''}</p></div></div><div className="hidden sm:flex items-center gap-3 text-[10px] text-slate-500 max-w-[55%] justify-end"><span className="inline-flex items-center gap-1.5 truncate"><FolderKanban size={13} aria-hidden="true" />{displayProjectName}</span>{displayClientName && <span className="inline-flex items-center gap-1.5 truncate"><UserRound size={13} aria-hidden="true" />{displayClientName}</span>}{displayEnvironment && <span className="truncate">{displayEnvironment}</span>}{displayStatus && <span className="inline-flex items-center gap-1.5 shrink-0"><CheckCircle2 size={13} aria-hidden="true" />{displayStatus}</span>}</div></header><ChatMessages messages={messages} isTyping={isTyping} onImageZoom={setActiveImageZoom} onArtifactOpen={openArtifact} messagesEndRef={messagesEndRef} error={error} onRetry={retryLast} onDismissError={dismissError} onSuggestion={(text) => setChatInput(text)} /><ChatInput chatInput={chatInput} setChatInput={handleChatInputChange} onSend={handleSend} onImageSelect={handleImageSelect} toggleRecording={toggleRecording} isListening={isListening} pendingUpload={pendingUpload} setPendingUpload={setPendingUpload} onSmartAction={onSmartAction} />{maskingImage && <div className="fixed inset-0 z-[300] bg-black/95 flex flex-col items-center justify-center p-4 animate-in fade-in duration-300"><div className="w-full max-w-sm aspect-[9/16] relative bg-slate-900 rounded-3xl overflow-hidden shadow-2xl border border-white/10"><img src={maskingImage.src} className="absolute inset-0 w-full h-full object-contain pointer-events-none" alt="Imagem do ambiente" /><canvas ref={canvasRef} onMouseDown={startDraw} onMouseMove={moveDraw} onMouseUp={endDraw} onTouchStart={startDraw} onTouchMove={moveDraw} onTouchEnd={endDraw} className="absolute inset-0 w-full h-full touch-none cursor-crosshair" /><div className="absolute top-6 left-6 right-6 flex justify-between items-center bg-black/40 backdrop-blur-md p-4 rounded-2xl border border-white/5"><span className="text-white text-xs font-bold">Marque a área do móvel</span><button type="button" aria-label="Fechar marcação" onClick={() => setMaskingImage(null)} className="text-white/70 hover:text-white p-2 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white"><X size={20}/></button></div></div><div className="mt-8 flex gap-4 w-full max-w-sm"><button type="button" onClick={() => setMaskingImage(null)} className="flex-1 py-4 bg-white/5 text-white/70 rounded-2xl font-bold text-xs hover:bg-white/10 transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white">Cancelar</button><button type="button" onClick={confirmMask} className="flex-1 py-4 bg-primary text-white rounded-2xl font-bold text-xs shadow-xl shadow-primary/20 hover:opacity-90 active:scale-95 transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary">Confirmar marcação</button></div></div>}{activeImageZoom && <div className="fixed inset-0 z-[300] bg-black/95 flex items-center justify-center p-4 animate-in zoom-in-95 duration-200" onClick={() => setActiveImageZoom(null)}><div className="relative max-w-4xl w-full" onClick={e => e.stopPropagation()}><img src={activeImageZoom.url} className="w-full h-auto rounded-2xl shadow-2xl" alt="Imagem ampliada" /><button type="button" onClick={() => setActiveImageZoom(null)} aria-label="Fechar imagem ampliada" className="absolute -top-4 -right-4 p-3 bg-white text-black rounded-full shadow-2xl hover:scale-110 active:scale-90 transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white"><X size={24}/></button>{activeImageZoom.budget && <div className="absolute bottom-6 left-6 bg-primary/90 backdrop-blur-lg px-6 py-3 rounded-2xl border border-white/20 shadow-2xl"><span className="text-white font-bold tracking-tight text-xl">R$ {activeImageZoom.budget}</span></div>}</div></div>}<ContextPanel panel={contextPanel} onClose={() => setContextPanel(null)} /><AuthDialog isOpen={showAuthDialog} onClose={() => setShowAuthDialog(false)} onSuccess={() => setShowAuthDialog(false)} /></div>);
};
export default IaraModule;
