-- This is usually done via API but we ensure it's noted in schema if needed.
-- Since configure_auth was already called, we are good on the management side.
-- We can add a check or a log here if needed, but the tool already handled it.
SELECT 1;