import { createClient } from "npm:@supabase/supabase-js@2";
import { serve } from "https://deno.land/std@0.168.0/http/server.ts";
const cors={"Content-Type":"application/json","Access-Control-Allow-Origin":"*"};
serve(async(req)=>{if(req.method==="OPTIONS")return new Response(null,{headers:cors});if(req.method!=="POST")return new Response(JSON.stringify({error:"Method not allowed"}),{status:405,headers:cors});try{const expected=Deno.env.get("ASAAS_WEBHOOK_TOKEN")?.trim(),received=req.headers.get("asaas-access-token")?.trim();if(!expected||!received||expected!==received)return new Response(JSON.stringify({error:"Webhook não autorizado."}),{status:401,headers:cors});const event=await req.json();if(!event?.id||!event?.event)return new Response(JSON.stringify({error:"Evento Asaas inválido."}),{status:400,headers:cors});const url=Deno.env.get("SUPABASE_URL"),key=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");if(!url||!key)return new Response(JSON.stringify({error:"Configuração do servidor incompleta."}),{status:500,headers:cors});const admin=createClient(url,key,{auth:{persistSession:false}}),payment=event.payment??{},subscriptionId=payment.subscription??event.subscription?.id??null;const{error}=await admin.from("asaas_webhook_events").insert({event_id:event.id,event_type:event.event,payment_id:payment.id??null,customer_id:payment.customer??event.customer?.id??null,subscription_id:subscriptionId,payload:event});if(error&&!String(error.message).toLowerCase().includes("duplicate")){console.error("Asaas webhook persistence error",error);return new Response(JSON.stringify({error:"Não foi possível registrar o webhook."}),{status:500,headers:cors})}if(event.subscription?.id&&event.subscription?.status)await admin.from("billing_subscriptions").update({status:String(event.subscription.status),updated_at:new Date().toISOString()}).eq("asaas_subscription_id",String(event.subscription.id));if(payment.id){const status=String(payment.status??event.event??"PENDING"),receivedStatus=["RECEIVED","CONFIRMED","RECEIVED_IN_CASH"].includes(status)||event.event==="PAYMENT_RECEIVED";const{data:purchase}=await admin.from("billing_purchases").select("id,user_id,credit_type,credits,status").eq("asaas_payment_id",String(payment.id)).maybeSingle();if(purchase){await admin.from("billing_purchases").update({status,updated_at:new Date().toISOString()}).eq("id",purchase.id);if(receivedStatus&&purchase.status!=="RECEIVED")await admin.rpc("grant_billing_credits",{p_user_id:purchase.user_id,p_credit_type:purchase.credit_type,p_credits:purchase.credits})}}return new Response(JSON.stringify({received:true,duplicate:Boolean(error)}),{status:200,headers:cors})}catch(error){console.error("Asaas webhook error",error);return new Response(JSON.stringify({error:"Falha ao processar webhook."}),{status:500,headers:cors})}});