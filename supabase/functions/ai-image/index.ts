import { createClient } from "npm:@supabase/supabase-js@2";
import { serve } from "https://deno.land/std@0.168.0/http/server.ts";
import { z } from "https://deno.land/x/zod@v3.23.8/mod.ts";
import { buildCorsHeaders, guardRequest } from "../_shared/guard.ts";

const MAX_BODY_BYTES=20*1024*1024,MIN_DIM=64,MAX_DIM=4096,DEFAULT_DIM=1024,MAX_PROMPT_CHARS=4000,MAX_PROMPT_WORDS=800,OPERATION_TYPE="gerarRender";
const ImageSchema=z.object({mimeType:z.string().regex(/^image\/(png|jpeg|jpg|webp|gif)$/i),data:z.string().min(1).max(15_000_000)});
const BodySchema=z.object({prompt:z.string().min(1),images:z.array(ImageSchema).max(8).optional(),size:z.object({width:z.number().int().min(MIN_DIM).max(MAX_DIM).optional(),height:z.number().int().min(MIN_DIM).max(MAX_DIM).optional()}).optional(),idempotencyKey:z.string().trim().min(8).max(200)});
const adminClient=()=>{const url=Deno.env.get("SUPABASE_URL"),key=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");if(!url||!key)throw new Error("server_config_incomplete");return createClient(url,key,{auth:{persistSession:false}})};
const errorResponse=(cors:Record<string,string>,status:number,body:unknown)=>new Response(JSON.stringify(body),{status,headers:{...cors,"Content-Type":"application/json"}});
async function refund(userId:string,key:string){await adminClient().rpc("refund_billing_credit",{p_user_id:userId,p_operation_type:OPERATION_TYPE,p_idempotency_key:key});}
serve(async req=>{const cors=buildCorsHeaders(req);if(req.method==="OPTIONS")return new Response(null,{headers:cors});if(req.method!=="POST")return errorResponse(cors,405,{message:"Method not allowed",code:"method_not_allowed"});const guard=await guardRequest(req,cors,{fn:"ai-image",limit:10,windowSeconds:60});if(!guard.ok)return guard.response;try{const length=Number(req.headers.get("content-length")??0);if(length>MAX_BODY_BYTES)return errorResponse(cors,413,{message:"Request body too large.",code:"payload_too_large"});const raw=await req.text();if(raw.length>MAX_BODY_BYTES)return errorResponse(cors,413,{message:"Request body too large.",code:"payload_too_large"});let body:unknown;try{body=JSON.parse(raw||"{}")}catch{return errorResponse(cors,400,{message:"Invalid JSON body",code:"invalid_json"})}const parsed=BodySchema.safeParse(body);if(!parsed.success)return errorResponse(cors,400,{message:"Validation failed",code:"validation_error",fields:parsed.error.flatten().fieldErrors});const{prompt:rawPrompt,images,size,idempotencyKey}=parsed.data;const prompt=rawPrompt.trim();if(prompt.length===0||prompt.length>MAX_PROMPT_CHARS)return errorResponse(cors,400,{message:"Validation failed",code:"validation_error",fields:{prompt:["prompt length invalid"]}});const wordCount=prompt.split(/\s+/).filter(Boolean).length;if(wordCount>MAX_PROMPT_WORDS)return errorResponse(cors,400,{message:"Validation failed",code:"validation_error",fields:{prompt:["prompt has too many words"]}});let width=DEFAULT_DIM,height=DEFAULT_DIM;if(size&&(size.width!==undefined||size.height!==undefined)){width=size.width??size.height??DEFAULT_DIM;height=size.height??size.width??DEFAULT_DIM}const admin=adminClient(),{data:consumed,error:consumeError}=await admin.rpc("consume_billing_credit",{p_user_id:guard.userId,p_operation_type:OPERATION_TYPE,p_idempotency_key:idempotencyKey});if(consumeError){const missing=consumeError.message.includes("commercial_rule_missing"),insufficient=consumeError.message.includes("insufficient_credits");return errorResponse(cors,402,{message:missing?"Esta operação ainda não possui uma regra comercial configurada.":insufficient?"Créditos insuficientes para gerar o render.":"Não foi possível autorizar o consumo de créditos.",code:missing?"commercial_rule_missing":insufficient?"insufficient_credits":"credit_authorization_failed"})}const key=Deno.env.get("GOOGLE_GEMINI_API_KEY");if(!key){await refund(guard.userId,idempotencyKey);return errorResponse(cors,500,{message:"GOOGLE_GEMINI_API_KEY is not configured",code:"missing_api_key"})}const parts:Array<Record<string,unknown>>=[{text:prompt}];for(const img of images??[])parts.push({inlineData:{mimeType:img.mimeType,data:img.data}});const model="gemini-2.5-flash-image";const response=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${model}:generateContent?key=${key}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({contents:[{role:"user",parts}],generationConfig:{responseModalities:["TEXT","IMAGE"]}})});if(!response.ok){await refund(guard.userId,idempotencyKey);const status=response.status===429?429:502;return errorResponse(cors,status,{message:response.status===429?"Rate limit exceeded. Tente novamente em alguns segundos.":`Gemini error: ${response.status}`,code:response.status===429?"rate_limited":"upstream_error"})}const data=await response.json(),candidate=data.candidates?.[0]?.content?.parts??[];let imageUrl:string|null=null,text:string|null=null;for(const part of candidate){const inline=part.inlineData||part.inline_data;if(inline)imageUrl=`data:${inline.mimeType||inline.mime_type};base64,${inline.data}`;if(part.text)text=part.text}if(!imageUrl){await refund(guard.userId,idempotencyKey);return errorResponse(cors,502,{message:"O provedor não retornou uma imagem.",code:"empty_image_result"})}return new Response(JSON.stringify({imageUrl,text,width,height,operationType:OPERATION_TYPE,creditConsumption:Array.isArray(consumed)?consumed[0]:consumed,promptStats:{wordCount,charCount:prompt.length,tokenEstimate:Math.ceil(prompt.length/4)}}),{headers:{...cors,"Content-Type":"application/json"}})}catch(e){console.error("ai-image error:",e);return errorResponse(cors,500,{message:"Erro interno ao gerar imagem.",code:"internal_error"})}});
